{
  "name": "sentiment-analyzer",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo para o Analisador de Sentimentos",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "start": "cd apps/web && npm run start",
    "pre-build": "npm install && npm run build"
  },
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.5.3",
    "@types/node": "^20.14.12"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "patch-package": "^8.0.0"
  }
}